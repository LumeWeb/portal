name: Build

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  build:
    uses: LumeWeb/workflows/.github/workflows/build-portal.yml@develop
    secrets: inherit